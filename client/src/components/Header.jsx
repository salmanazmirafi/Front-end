import { AppBar, Toolbar, Typography } from "@mui/material";

function Header() {
  return (
    <AppBar>
      <Toolbar>
        <Typography>Hi</Typography>
      </Toolbar>
    </AppBar>
  );
}

export default Header;
